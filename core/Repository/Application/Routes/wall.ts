import { Request, Response } from 'express';
import express from 'express';

const wall = express.Router();

wall.get('/world/api/v1/post/list', (_req: Request, _res:Response) => {

});

wall.post('/world/api/v1/post', (_req: Request, _res: Response) => {

});
